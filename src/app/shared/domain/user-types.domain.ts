export enum UserTypes {
  ADMIN,
  STUDENT,
  DRIVER,
}

export enum UserTypesIDs {
  ADMIN = 3,
  STUDENT = 4,
  DRIVER = 5,
  NOT_FOUND = -1,
}

export enum UserGenders {
  MALE = 'M',
  FEMALE = 'F',
  NON_BINARY = 'NB',
  NOT_INFORMED = 'NI',
}
